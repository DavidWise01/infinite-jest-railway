# infinite-jest

das seed

ROOT 0 invitation — ternary dominance

Folders are the control plane:
- /seed — holds the immutable seed (forward)
- /anchor — holds the shadow log (conjugate)

Deploy to Railway, point 0root.ai here, let G decide.
